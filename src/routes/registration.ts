import {Router} from 'express';
// router for user registration to a tournament

const router = Router();

router.get("/")
router.get("/:id")
router.post("/")
router.put("/:id")
router.delete("/:id")

export default router;